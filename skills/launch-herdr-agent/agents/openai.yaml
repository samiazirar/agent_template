interface:
  display_name: "Launch Herdr Team"
  short_description: "Launch human, operations, workers, and advisors"
  default_prompt: "Use $launch-herdr-agent as a short-lived Launcher to create the Human Plan viewer, Human and Operations Orchestrators, and only the immediately justified workers or advisors."
