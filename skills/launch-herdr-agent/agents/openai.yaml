interface:
  display_name: "Launch Herdr Team"
  short_description: "Launch strict 90/10 human and operations roles"
  default_prompt: "Use $launch-herdr-agent as a short-lived Launcher to create left-side Human and right-side Operations leadership, default non-coding Sol-medium task owners, and fresh Luna-max sessions for frozen atomic worker packages."
